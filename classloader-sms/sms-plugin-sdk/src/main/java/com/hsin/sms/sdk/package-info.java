/**
 * Optional helper API for SMS plugin developers.
 *
 * <p>The SDK depends only on {@code sms-spi}. Hosts resolve it parent-first, so a
 * plugin can use it without bundling it into {@code lib/}.</p>
 */
package com.hsin.sms.sdk;
